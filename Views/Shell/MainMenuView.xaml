<UserControl x:Class="Project.DesktopClient.Views.Shell.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project.DesktopClient.Views.Shell"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Background="LightGray">

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            
            <Button Content="Usuários" Margin="10"
                    Width="100" Height="50"
                    Command="{Binding NavigateUserCommand}"/>
            
            <Button Content="Reviews" Margin="10"
                    Width="100" Height="50"
                    Command="{Binding NavigateReviewCommand}"/>
        </StackPanel>            
    </Grid>
</UserControl>
