<UserControl x:Class="Project.DesktopClient.Views.User.UserMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project.DesktopClient.Views.User"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Usuários" Grid.Row="0"
            FontSize="30" FontWeight="Bold"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"/>

        <DataGrid Grid.Row="1" Grid.Column="1" Margin="15"
            ItemsSource="{Binding Users}"
            AutoGenerateColumns="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"
                    Binding="{Binding Id}" Width="50"/>

                <DataGridTextColumn Header="Nome"
                    Binding="{Binding FirstName}" Width="150"/>

                <DataGridTextColumn Header="Sobrenome"
                    Binding="{Binding LastName}" Width="150"/>

                <DataGridTextColumn Header="Idade"
                    Binding="{Binding Age}" Width="50"/>

                <DataGridTextColumn Header="Email"
                    Binding="{Binding Email}" Width="200"/>

                <DataGridTemplateColumn Header="Ações" Width="auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>

                            <StackPanel Orientation="Horizontal">
                                <Button Content="Atualizar"/>
                                <Button Content="Deletar"/>
                            </StackPanel>

                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2"                    
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top">
            
            <StackPanel Orientation="Horizontal">

                <Button Content="Novo" Margin="15"
                    Width="75" Height="50"
                    Command="{Binding OpenCreateDialogCommand}"/>

                <Button Content="Atualizar" Margin="15"
                    Width="75" Height="50"
                    Command="{Binding LoadUsersCommand}"/>
                
            </StackPanel>
            
            <Button Content="Voltar"
                    Command="{Binding NavigateMenuCommand}"
                    Width="75" Height="30"/>
        </StackPanel>

    </Grid>
</UserControl>
