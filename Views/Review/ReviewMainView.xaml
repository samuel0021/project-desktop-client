<UserControl x:Class="Project.DesktopClient.Views.Review.ReviewMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project.DesktopClient.Views.Review"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Reviews" Grid.Row="0"
        FontSize="30" FontWeight="Bold"
        HorizontalAlignment="Center"
        VerticalAlignment="Top"/>

        <DataGrid Grid.Row="1" Grid.Column="1" Margin="15"
        ItemsSource="{Binding Reviews}"
        SelectedItem="{Binding SelectedReview}"
        AutoGenerateColumns="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"
                Binding="{Binding Id}" Width="50"/>

                <DataGridTextColumn Header="Título"
                Binding="{Binding Title}" Width="150"/>

                <DataGridTextColumn Header="Categoria"
                Binding="{Binding Category}" Width="150"/>
                
                <DataGridTextColumn Header="UserId"
                Binding="{Binding UserId}" Width="150"/>

                <DataGridTextColumn Header="UserName"
                Binding="{Binding UserName}" Width="150"/>

                <DataGridTemplateColumn Header="Ações" Width="auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>

                            <StackPanel Orientation="Horizontal">
                                <Button Content="Visualizar"
                                Command="{Binding DataContext.OpenVisualizeDialogCommand, 
                                RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                
                                <Button Content="Atualizar"
                                Command="{Binding DataContext.OpenEditDialogCommand, 
                                RelativeSource={RelativeSource AncestorType=UserControl}}"/>

                                <Button Content="Deletar"
                                    Command="{Binding DataContext.OpenDeleteDialogCommand, 
                                RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                            </StackPanel>

                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2"                    
                HorizontalAlignment="Center"
                VerticalAlignment="Top">

            <StackPanel Orientation="Horizontal">

                <Button Content="Novo" Margin="15"
                Width="75" Height="50"
                Command="{Binding OpenCreateDialogCommand}"/>

                <Button Content="Atualizar lista" Margin="15"
                Width="75" Height="50"
                Command="{Binding LoadReviewsCommand}"/>

            </StackPanel>

            <Button Content="Voltar"
                Command="{Binding NavigateMenuCommand}"
                Width="75" Height="30"/>
        </StackPanel>

    </Grid>
</UserControl>
